// Typescript with Functions

const add = (a: number, b: number) : number => {
  return a + b;
}

console.log('Output is ' + add(5, 3));

const joinStrings = (a: string, b: string): string => {
  return a + b;
};
console.log('Alex ' + 'Bobek');